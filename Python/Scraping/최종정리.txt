1) HTML 뼈대, CSS, JS
2) XPath   //*[@id="Search할 text"]
3) chrome 개발자 도구활용
4) 정규식 Re , . ^ $ match search findall
5) User-Agent : 서버에서 이 정보를 받아서 클라이언트에게 정보를 준다
6) Requests : 빠르다. 동적이이 못하다
    res.raise_for_status()
7) Selenium : 동적인 페이지에서 클릭등을 처리할 수 있다
    크롬 버전에 맞는 chromedriver.exe가 있어야 한다.
    find_element(s)_by_~~~
    click()
8)    send_keys()    
9)때로는 기다려야 한다.
    elem=WebDriverWait(browser,10).
        until(EC.presence_of_element_located((BY.XPath, "xpath")))

10) BeautifulSoup : 6), 7)의 정보를 받아서
    find
    find_all
    find_next()

    soup["속성]
    soup.get_text()

11) fopen
12) csv  encoding="utf-8-sig",  anci?

13) Headless chrome 
    User-Agent사용법

퀴즈. 부동산 매물 정보를 다음에서 검색해서, 다음부동산의 전체 정보를 가져오자.    



